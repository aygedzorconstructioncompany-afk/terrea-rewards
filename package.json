{
  "name": "terrea-rewards",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "author": "info",
  "description": "Shopify Terrea Rewards App",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start",
    "shopify": "shopify",
    "deploy": "shopify app deploy",
    "env": "shopify app env"
  },
  "engines": {
    "node": ">=20.19 <22 || >=22.12"
  },
  "workspaces": [
    "extensions/*",
    "web"
  ],
  "prisma": {
    "schema": "web/prisma/schema.prisma"
  },
  "devDependencies": {
    "@prisma/client": "^6.19.2",
    "prisma": "^6.19.2",
    "vite": "^8.0.3"
  }
}
